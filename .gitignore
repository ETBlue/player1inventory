# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.vite
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static
storybook-static

# Git worktrees
.worktrees

# Agent scratch workspaces (task briefs, reports, review diffs)
.superpowers

# Claude Code local settings (personal approval history, not for sharing)
.claude/settings.local.json

# Playwright
playwright-report
test-results

# Environment variables
apps/server/.env

# Parked migration directory used by apps/server/scripts/verify-migration.ts —
# should never exist outside of an interrupted run, but must not be committed
# if it does.
apps/server/.migration-under-test/

# Generated by graphql-codegen
apps/server/src/generated/
apps/web/src/generated/
